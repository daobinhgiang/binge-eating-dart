import 'package:flutter/material.dart';

class Lesson94Screen extends StatelessWidget {
  const Lesson94Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 9.4: An Effective First Step: Guided Self-Help'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

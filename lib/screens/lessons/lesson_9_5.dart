import 'package:flutter/material.dart';

class Lesson95Screen extends StatelessWidget {
  const Lesson95Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 9.5: The Recommended Path to Recovery: Stepped Care'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

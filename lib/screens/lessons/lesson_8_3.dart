import 'package:flutter/material.dart';

class Lesson83Screen extends StatelessWidget {
  const Lesson83Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 8.3: Why This Distinction Is VITAL for Treatment'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Lesson122Screen extends StatelessWidget {
  const Lesson122Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 12.2: Part 2: Addressing Purging Behaviors'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Lesson71Screen extends StatelessWidget {
  const Lesson71Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 7.1: A Crucial Distinction: Starting vs Continuing'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

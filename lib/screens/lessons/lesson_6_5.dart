import 'package:flutter/material.dart';

class Lesson65Screen extends StatelessWidget {
  const Lesson65Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 6.5: At a Glance: The Ineffectiveness of Purging'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

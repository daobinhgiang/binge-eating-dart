import 'package:flutter/material.dart';

class Lesson103Screen extends StatelessWidget {
  const Lesson103Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 10.3: Is This Program Right for You? When to Start and When to Pause'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

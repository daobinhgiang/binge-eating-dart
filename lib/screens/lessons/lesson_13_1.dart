import 'package:flutter/material.dart';

class Lesson131Screen extends StatelessWidget {
  const Lesson131Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 13.1: Part 1: Prepare Your List of Alternative Activities'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

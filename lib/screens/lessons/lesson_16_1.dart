import 'package:flutter/material.dart';

class Lesson161Screen extends StatelessWidget {
  const Lesson161Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 16.1: Part 1: Are You a Strict Dieter?'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

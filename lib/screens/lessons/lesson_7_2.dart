import 'package:flutter/material.dart';

class Lesson72Screen extends StatelessWidget {
  const Lesson72Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 7.2: Why Binge Eating Problems Start: A Mix of Risk Factors'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

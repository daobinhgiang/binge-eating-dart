import 'package:flutter/material.dart';

class Appendix24Screen extends StatelessWidget {
  const Appendix24Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Appendix 2.4: Part 4: Health Risks of a High BMI'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

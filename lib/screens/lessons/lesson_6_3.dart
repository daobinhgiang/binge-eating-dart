import 'package:flutter/material.dart';

class Lesson63Screen extends StatelessWidget {
  const Lesson63Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 6.3: The Dangers of Compensatory Behaviors ("Purging")'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Appendix11Screen extends StatelessWidget {
  const Appendix11Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Appendix 1.1: Part 1: Why Finding the Right Professional Matters'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

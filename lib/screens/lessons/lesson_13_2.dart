import 'package:flutter/material.dart';

class Lesson132Screen extends StatelessWidget {
  const Lesson132Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 13.2: Part 2: "Urge Surfing" – Substituting an Alternative Activity'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Appendix41Screen extends StatelessWidget {
  const Appendix41Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Appendix 4.1: Part 1: Should I Seek Professional Help or Use Self-Help?'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Lesson171Screen extends StatelessWidget {
  const Lesson171Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 17.1: Part 1: Identifying Overconcern with Shape and Weight'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Appendix22Screen extends StatelessWidget {
  const Appendix22Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Appendix 2.2: Part 2: How to Calculate Your BMI'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

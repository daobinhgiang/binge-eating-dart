import 'package:flutter/material.dart';

class Lesson73Screen extends StatelessWidget {
  const Lesson73Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 7.3: Why Binge Eating Problems Continue: The Vicious Maintenance Cycles'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

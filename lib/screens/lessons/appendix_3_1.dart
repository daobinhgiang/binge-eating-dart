import 'package:flutter/material.dart';

class Appendix31Screen extends StatelessWidget {
  const Appendix31Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Appendix 3.1: Part 1: Address the Eating Problem First'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}

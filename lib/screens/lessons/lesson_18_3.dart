import 'package:flutter/material.dart';

class Lesson183Screen extends StatelessWidget {
  const Lesson183Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lesson 18.3: Part 3: How to Deal with Setbacks'),
      ),
      body: const Center(
        child: Text(
          'Content coming soon...',
          style: TextStyle(
            fontSize: 18,
            color: Colors.grey,
          ),
        ),
      ),
    );
  }
}
